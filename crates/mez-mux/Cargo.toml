[package]
name = "mez-mux"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
mez-core = { path = "../mez-core", version = "0.1.0" }
mez-terminal = { path = "../mez-terminal", version = "0.1.0" }
portable-pty = "0.9"
pulldown-cmark = "0.13"
rustix = { version = "1.1", features = ["event", "fs", "process", "termios"] }
shlex = "2"
syntect = { version = "5.3", default-features = false, features = ["default-syntaxes", "parsing", "regex-fancy"] }
thiserror = "2"
unicode-width = "0.2"
unicode-segmentation = "1.12"

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2"

[target.'cfg(target_os = "linux")'.dev-dependencies]
libc = "0.2"
