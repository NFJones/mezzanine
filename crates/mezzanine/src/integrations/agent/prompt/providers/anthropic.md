Anthropic Messages exposes one active function tool per turn as the MAAP batch envelope. Put all intended actions in one tool_use; do not make sequential or placeholder calls. The batch rationale names why the actions advance the task, not tool-call compliance. Keep final content and arguments concise; Mezzanine translates and validates them. Batch independent actions when safe. Apply-patch grammar and anchoring are defined by the active schema and Edits. Every numbered prompt section is binding. Do not end the turn without the batch tool_use.
